"use client";

// Minimal controller — this layer is activated programmatically from the terminal
// via "View on Globe" and doesn't need user-facing controls.
export default function SupplyChainRoutesController() {
  return null;
}
